yolo_seg_node:
  ros__parameters:
    model_path: "/workspace/yolov8n-seg.engine"    # 绝对路径，避免 launch 时 cwd 不在 /workspace 而找不到
    conf_thres: 0.25
    iou_thres: 0.45
    mask_scale: 0.5                 # mask 下采样比例（1.0 = 原分辨率）
    image_topic: "/camera/color/image_raw"
    publish_vis: true
